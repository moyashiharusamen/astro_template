---
import { v4 as uuidv4 } from 'uuid';

export interface Props {
  text: Array<string>;
}

const { text } = Astro.props;
---

<div class="root textTransition">
  {text.map(item => <p class="textTransition__item">{item}</p>)}
</div>

<style lang="scss">
  @use '../../style/global' as g;

  // ---------- 基底要素 ----------
  .root {
  }
</style>
